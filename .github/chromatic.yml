name: Chromatic Deployment

on: pull_request

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - run: yarn

      - uses: chromaui/action@v1
        with:
          projectToken: ${{secrets.CROMATIC_PROJECT_TOKEN}}
          token: ${{secrets.GITHUB_TOKEN}}
